baseURL = "https://gimsuga.github.io/prototypes/"
locale = "en-NG"
title = "GIMSUGA website prototypes"
disableKinds = ["taxonomy", "term", "rss", "sitemap", "robotstxt"]

[markup.goldmark.renderer]
  unsafe = false

[params]
  # Fictional prototype. Nothing here is real (G-011, G-012, G-021).
  chapter = "abakaliki"
